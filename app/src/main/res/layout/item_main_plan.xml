<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@color/background_recently_plan">

    <ImageView
        android:id="@+id/iv_month"
        android:layout_width="40dp"
        android:layout_height="45dp"
        android:layout_marginStart="10dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        tools:ignore="ContentDescription,MissingConstraints"
        tools:src="@drawable/ic_march" />

    <TextView
        android:id="@+id/tv_plan"
        style="@style/WithTextView"
        android:layout_marginStart="10dp"
        android:layout_marginTop="20dp"
        android:textSize="15sp"
        android:textStyle="bold"
        app:layout_constraintStart_toEndOf="@id/iv_month"
        app:layout_constraintTop_toTopOf="parent"
        tools:ignore="RtlSymmetry"
        tools:text="풋살" />

    <TextView
        android:id="@+id/tv_date_time"
        style="@style/WithTextView"
        app:layout_constraintStart_toStartOf="@id/tv_plan"
        app:layout_constraintTop_toBottomOf="@id/tv_plan"
        tools:text="2019-05-05 8PM" />

    <TextView
        android:id="@+id/tv_place"
        style="@style/WithTextView"
        android:layout_marginBottom="20dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="@id/tv_plan"
        app:layout_constraintTop_toBottomOf="@id/tv_date_time"
        tools:text="스포원파크" />

    <TextView
        android:id="@+id/tv_decimal_day"
        style="@style/WithTextView"
        android:layout_marginEnd="10dp"
        android:textSize="15sp"
        android:textStyle="bold"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        tools:text="D-50" />

</android.support.constraint.ConstraintLayout>